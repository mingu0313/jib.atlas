// open-next.config.ts created for Cloudflare deployment via @opennextjs/cloudflare
// R2 incremental cache / image optimization bindings can be added later
// (see https://opennext.js.org/cloudflare/caching, /howtos/image) once needed.
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({});
